<!-- we can target with the div id="posts" on the index page --> 
<%#= update_list%>

<%= turbo_stream.replace "lists" do %>
    <%= render partial: "update_lists", locals: { board: board, list_updated: list, position: 380*(position.to_i)} %>
    <%#= render @list %>  
<% end %>



