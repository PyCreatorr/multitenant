<!-- we can target with the div id="posts" on the index page --> 
<%#= update_list%>

<!-- Update the "lists" + shift the y-scroll to the new position --> 
<%= turbo_stream.replace "lists" do %>
    <%= render partial: "lists/update_lists", locals: { board: board, list_updated: list, position: 380*(position.to_i)} %>
    <%#= render @list %>  
<% end %>



